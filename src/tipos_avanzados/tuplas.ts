export const userID: (number | string)[] = [1, 'uno'];
userID.push(2);
userID.push('dos');
userID.push('dos');

export const user_id: [number, string] = [1, 'uno'];

export const [db_sql, db_nosql] = user_id;
